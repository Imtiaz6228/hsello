<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Balance & Withdrawals - DigitalMarket Seller</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <link href="/css/style.css" rel="stylesheet">
    <style>
        .balance-container {
            max-width: 1200px;
            margin: 120px auto 40px;
            padding: 0 20px;
        }
        .balance-card {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            border-radius: 15px;
            padding: 30px;
            margin-bottom: 30px;
            box-shadow: 0 10px 30px rgba(102, 126, 234, 0.3);
        }
        .balance-amount {
            font-size: 3em;
            font-weight: 700;
            margin-bottom: 10px;
        }
        .balance-label {
            opacity: 0.9;
            font-size: 1.1em;
        }
        .stats-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 20px;
            margin-bottom: 30px;
        }
        .stat-card {
            background: white;
            border-radius: 12px;
            padding: 25px;
            box-shadow: 0 4px 15px rgba(0,0,0,0.08);
            text-align: center;
        }
        .stat-number {
            font-size: 2.5em;
            font-weight: 700;
            color: #2c3e50;
            margin-bottom: 10px;
        }
        .stat-label {
            color: #7f8c8d;
            font-weight: 600;
            text-transform: uppercase;
            letter-spacing: 1px;
        }
        .withdraw-section {
            background: white;
            border-radius: 15px;
            padding: 30px;
            box-shadow: 0 4px 15px rgba(0,0,0,0.08);
            margin-bottom: 30px;
        }
        .withdraw-form {
            max-width: 500px;
        }
        .form-group {
            margin-bottom: 20px;
        }
        .form-label {
            font-weight: 600;
            color: #2c3e50;
            margin-bottom: 8px;
        }
        .form-control {
            border: 2px solid #ecf0f1;
            border-radius: 8px;
            padding: 12px 15px;
            font-size: 1em;
        }
        .form-control:focus {
            border-color: #667eea;
            box-shadow: 0 0 0 0.2rem rgba(102, 126, 234, 0.25);
        }
        .withdraw-btn {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            border: none;
            padding: 15px 30px;
            border-radius: 8px;
            font-weight: 600;
            font-size: 1em;
            width: 100%;
            transition: transform 0.3s ease;
        }
        .withdraw-btn:hover {
            transform: translateY(-2px);
            box-shadow: 0 5px 15px rgba(102, 126, 234, 0.4);
        }
        .withdraw-btn:disabled {
            background: #95a5a6;
            cursor: not-allowed;
        }
        .withdrawals-list {
            background: white;
            border-radius: 15px;
            padding: 30px;
            box-shadow: 0 4px 15px rgba(0,0,0,0.08);
        }
        .withdrawal-item {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 20px;
            border: 1px solid #ecf0f1;
            border-radius: 8px;
            margin-bottom: 15px;
        }
        .withdrawal-item:last-child {
            margin-bottom: 0;
        }
        .withdrawal-info {
            flex: 1;
        }
        .withdrawal-id {
            font-weight: 700;
            color: #2c3e50;
            margin-bottom: 5px;
        }
        .withdrawal-date {
            color: #7f8c8d;
            font-size: 0.9em;
        }
        .withdrawal-status {
            padding: 6px 12px;
            border-radius: 20px;
            font-size: 0.85em;
            font-weight: 600;
        }
        .status-pending { background: #fff3cd; color: #856404; }
        .status-approved { background: #d4edda; color: #155724; }
        .status-rejected { background: #f8d7da; color: #721c24; }
        .status-processing { background: #cce5ff; color: #004085; }

        .empty-state {
            text-align: center;
            padding: 60px;
            color: #7f8c8d;
        }
        .empty-state i {
            font-size: 3em;
            margin-bottom: 20px;
            opacity: 0.5;
        }
        .warning-banner {
            background: linear-gradient(135deg, #f093fb 0%, #f5576c 100%);
            color: white;
            border-radius: 10px;
            padding: 20px;
            margin-bottom: 20px;
            text-align: center;
        }
        .min-withdrawal {
            background: #fff6e8;
            border-left: 4px solid #f39c12;
            padding: 15px;
            border-radius: 0 8px 8px 0;
            margin-bottom: 20px;
        }

        @media (max-width: 768px) {
            .balance-amount {
                font-size: 2.5em;
            }
            .stats-grid {
                grid-template-columns: 1fr;
            }
            .withdrawal-item {
                flex-direction: column;
                gap: 15px;
                text-align: center;
            }
        }
    </style>
</head>
<body>
    <%- include('../partials/navbar') %>

    <div class="balance-container">
        <div class="d-flex justify-content-between align-items-center mb-4">
            <h1><i class="fas fa-wallet me-3"></i>My Balance</h1>
            <a href="/seller/orders" class="btn btn-outline-primary">
                <i class="fas fa-shopping-bag me-2"></i>View Orders
            </a>
        </div>

        <!-- Current Balance -->
        <div class="balance-card">
            <div class="text-center">
                <div class="balance-amount">₽<%= balance.toFixed(2) %></div>
                <div class="balance-label">Available Balance</div>
            </div>
        </div>

        <!-- Statistics -->
        <div class="stats-grid">
            <div class="stat-card">
                <div class="stat-number"><%= withdrawals.filter(w => w.status === 'pending').length %></div>
                <div class="stat-label">Pending Withdrawals</div>
            </div>
            <div class="stat-card">
                <div class="stat-number"><%= withdrawals.filter(w => w.status === 'approved').length %></div>
                <div class="stat-label">Completed Withdrawals</div>
            </div>
            <div class="stat-card">
                <div class="stat-number">₽<%= withdrawals.filter(w => w.status === 'approved').reduce((sum, w) => sum + w.amount, 0).toFixed(2) %></div>
                <div class="stat-label">Total Withdrawn</div>
            </div>
        </div>

        <!-- Minimum Withdrawal Notice -->
        <% if (balance < 50) { %>
            <div class="min-withdrawal">
                <h6><i class="fas fa-info-circle me-2"></i>Minimum Withdrawal Amount</h6>
                <p class="mb-0">You need at least ₽50.00 in your balance to request a withdrawal. Current balance: ₽<%= balance.toFixed(2) %></p>
            </div>
        <% } %>

        <!-- Withdrawal Form -->
        <div class="withdraw-section">
            <h3 class="mb-4"><i class="fas fa-credit-card me-2"></i>Request Withdrawal</h3>

            <div class="row">
                <div class="col-lg-6">
                    <form action="/seller/withdrawal" method="POST" id="withdrawalForm">
                        <div class="form-group">
                            <label for="amount" class="form-label">Withdrawal Amount (₽)</label>
                            <input type="number" class="form-control" id="amount" name="amount"
                                   min="50" max="<%= balance %>" step="0.01"
                                   placeholder="Enter amount (minimum ₽50.00)"
                                   value="<%= balance >= 50 ? balance.toFixed(2) : '' %>" required>
                            <small class="text-muted">Available: ₽<%= balance.toFixed(2) %> | Min: ₽50.00</small>
                        </div>

                        <div class="form-group">
                            <label for="blockchain" class="form-label">Blockchain Network</label>
                            <select class="form-control" id="blockchain" name="blockchain" required>
                                <option value="">Select Blockchain</option>
                                <option value="USDT_TRC20" selected>USDT TRC20 (TRON)</option>
                                <option value="USDT_ERC20">USDT ERC20 (Ethereum)</option>
                                <option value="USDT_BEP20">USDT BEP20 (BNB Smart Chain)</option>
                                <option value="BTC">BTC (Bitcoin)</option>
                                <option value="ETH">ETH (Ethereum)</option>
                                <option value="BNB">BNB (Binance)</option>
                                <option value="ADA">ADA (Cardano)</option>
                                <option value="SOL">SOL (Solana)</option>
                                <option value="DOGE">DOGE (Dogecoin)</option>
                            </select>
                            <small class="text-muted">Select the blockchain network for your wallet</small>
                        </div>

                        <div class="form-group">
                            <label for="cryptoWalletAddress" class="form-label">Wallet Address</label>
                            <input type="text" class="form-control" id="cryptoWalletAddress"
                                   name="cryptoWalletAddress" placeholder="Enter your wallet address"
                                   pattern="[a-zA-Z0-9]+" title="Only letters and numbers allowed"
                                   required>
                            <small class="text-muted">Your funds will be sent to this address after admin approval</small>
                        </div>

                        <button type="submit" class="withdraw-btn" <%= balance < 50 ? 'disabled' : '' %>>
                            <i class="fas fa-paper-plane me-2"></i>Request Withdrawal
                        </button>
                    </form>
                </div>

                <div class="col-lg-6">
                    <div class="warning-banner">
                        <h5><i class="fas fa-shield-alt me-2"></i>Important Notice</h5>
                        <p class="mb-0">All withdrawals require admin approval and may take 1-3 business days to process. Make sure your crypto wallet address is correct.</p>
                    </div>

                    <div style="background: #f8f9fa; padding: 20px; border-radius: 8px; margin-top: 20px;">
                        <h6><i class="fas fa-clock me-2"></i>Processing Time</h6>
                        <ul class="list-unstyled mb-0">
                            <li><strong>Submission:</strong> Instant</li>
                            <li><strong>Admin Review:</strong> 1-2 business days</li>
                            <li><strong>Processing:</strong> 1-2 business days</li>
                            <li><strong>Total:</strong> 2-4 business days</li>
                        </ul>
                    </div>
                </div>
            </div>
        </div>

        <!-- Withdrawal History -->
        <div class="withdrawals-list">
            <h3 class="mb-4"><i class="fas fa-history me-2"></i>Withdrawal History</h3>

            <% if (withdrawals && withdrawals.length > 0) { %>
                <% withdrawals.forEach(withdrawal => { %>
                    <div class="withdrawal-item">
                        <div class="withdrawal-info">
                            <div class="withdrawal-id">#<%= withdrawal.id %></div>
                            <div class="withdrawal-date"><%= new Date(withdrawal.requestedAt).toLocaleString() %></div>
                            <div class="withdrawal-address">
                                <small class="text-muted">
                                    <i class="fas fa-wallet me-1"></i>
                                    <strong><%= withdrawal.blockchain || 'USDT_TRC20' %></strong> -
                                    <%= withdrawal.cryptoWalletAddress.substring(0, 10) %>...<%= withdrawal.cryptoWalletAddress.slice(-8) %>
                                </small>
                            </div>
                        </div>
                        <div class="text-end">
                            <div class="fw-bold fs-5 mb-2">₽<%= withdrawal.amount.toFixed(2) %></div>
                            <span class="withdrawal-status status-<%= withdrawal.status %>">
                                <%= withdrawal.status.charAt(0).toUpperCase() + withdrawal.status.slice(1) %>
                            </span>
                            <% if (withdrawal.transactionId) { %>
                                <div class="mt-2">
                                    <small class="text-muted">TXN: <%= withdrawal.transactionId %></small>
                                </div>
                            <% } %>
                        </div>
                    </div>
                <% }); %>
            <% } else { %>
                <div class="empty-state">
                    <i class="fas fa-credit-card"></i>
                    <h5>No Withdrawals Yet</h5>
                    <p>You haven't requested any withdrawals yet. Your earnings from completed orders will appear here.</p>
                </div>
            <% } %>
        </div>
    </div>

    <script>
        // Form validation
        document.getElementById('withdrawalForm').addEventListener('submit', function(e) {
            const amount = parseFloat(document.getElementById('amount').value);
            const available = parseFloat('<%= balance %>');

            if (amount > available) {
                e.preventDefault();
                alert('Withdrawal amount cannot exceed your available balance.');
                return;
            }

            if (amount < 50) {
                e.preventDefault();
                alert('Minimum withdrawal amount is ₽50.00');
                return;
            }

            const amountStr = amount.toFixed(2);
            if (!confirm('Are you sure you want to request a withdrawal of ₽' + amountStr + '? This will be processed after admin approval.')) {
                e.preventDefault();
            }
        });

        // Auto-limit max amount
        document.getElementById('amount').addEventListener('input', function() {
            const maxAvailable = parseFloat('<%= balance %>');
            if (parseFloat(this.value) > maxAvailable) {
                this.value = maxAvailable;
            }
        });
    </script>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
