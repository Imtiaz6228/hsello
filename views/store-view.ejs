<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title><%= store.name %> - DigitalMarket</title>
  <link rel="stylesheet" href="/css/style.css" />
  <style>
    .store-view-page {
      min-height: calc(100vh - 72px);
      background: linear-gradient(135deg, #f5f7fa 0%, #c3cfe2 100%);
      padding: 40px 20px;
      padding-top: 120px;
    }

    .store-view-container {
      max-width: 1000px;
      margin: 0 auto;
    }

    .store-banner {
      height: 200px;
      background-size: cover;
      background-position: center;
      border-radius: 15px;
      margin-bottom: 30px;
      position: relative;
      background-color: #f0f0f0;
      box-shadow: 0 8px 25px rgba(0,0,0,0.1);
    }

    .store-header {
      display: flex;
      align-items: center;
      margin-bottom: 30px;
      gap: 20px;
    }

    .store-logo {
      width: 80px;
      height: 80px;
      border-radius: 50%;
      background-size: cover;
      background-position: center;
      border: 4px solid white;
      box-shadow: 0 4px 15px rgba(0,0,0,0.1);
      background-color: #f0f0f0;
    }

    .store-info {
      flex: 1;
    }

    .store-name {
      font-size: 2em;
      font-weight: 700;
      color: #2c3e50;
      margin: 0 0 10px 0;
    }

    .store-meta {
      display: flex;
      gap: 20px;
      margin-bottom: 15px;
    }

    .store-category {
      background: #e8f5e8;
      color: #27ae60;
      padding: 6px 12px;
      border-radius: 15px;
      font-weight: 600;
      text-transform: capitalize;
    }

    .store-seller {
      color: #7f8c8d;
      font-size: 0.9em;
    }

    .store-description {
      color: #34495e;
      line-height: 1.6;
      margin-bottom: 15px;
    }

    .store-content {
      display: grid;
      grid-template-columns: 2fr 1fr;
      gap: 30px;
    }

    .store-details {
      background: white;
      border-radius: 15px;
      padding: 30px;
      box-shadow: 0 8px 25px rgba(0,0,0,0.1);
    }

    .detail-section {
      margin-bottom: 25px;
    }

    .detail-section h3 {
      color: #2c3e50;
      font-size: 1.2em;
      margin: 0 0 15px 0;
      font-weight: 700;
    }

    .detail-section p {
      margin: 5px 0;
      color: #34495e;
      line-height: 1.5;
    }

    .contact-info {
      background: #f8f9fa;
      border-radius: 8px;
      padding: 15px;
      margin-top: 10px;
    }

    .contact-info strong {
      color: #2c3e50;
    }

    .sidebar {
      display: flex;
      flex-direction: column;
      gap: 20px;
    }

    .sidebar-card {
      background: white;
      border-radius: 15px;
      padding: 20px;
      box-shadow: 0 8px 25px rgba(0,0,0,0.1);
    }

    .sidebar-card h3 {
      margin: 0 0 15px 0;
      color: #2c3e50;
      font-size: 1.1em;
      font-weight: 700;
    }

    .stat-item {
      display: flex;
      justify-content: space-between;
      align-items: center;
      padding: 10px 0;
      border-bottom: 1px solid #ecf0f1;
    }

    .stat-item:last-child {
      border-bottom: none;
    }

    .stat-number {
      font-size: 1.5em;
      font-weight: 700;
      color: #667eea;
    }

    .stat-label {
      color: #7f8c8d;
      font-weight: 600;
    }

    .action-buttons {
      display: flex;
      flex-direction: column;
      gap: 10px;
    }

    .action-btn {
      padding: 12px 20px;
      border: none;
      border-radius: 8px;
      font-weight: 600;
      font-size: 0.9em;
      cursor: pointer;
      text-decoration: none;
      display: inline-block;
      text-align: center;
      transition: all 0.3s ease;
    }

    .action-btn.primary {
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      color: white;
    }

    .action-btn.primary:hover {
      transform: translateY(-2px);
      box-shadow: 0 5px 15px rgba(102, 126, 234, 0.4);
    }

    .action-btn.secondary {
      background: #27ae60;
      color: white;
    }

    .action-btn.secondary:hover {
      transform: translateY(-2px);
      box-shadow: 0 5px 15px rgba(39, 174, 96, 0.4);
    }

    .store-rules {
      background: #fff6e8;
      border-left: 4px solid #f39c12;
      padding: 15px;
      margin-top: 15px;
      border-radius: 0 8px 8px 0;
    }

    .store-rules h4 {
      margin: 0 0 10px 0;
      color: #f39c12;
      font-weight: 700;
    }

    .store-rules p {
      margin: 0;
      color: #8b6f47;
      line-height: 1.5;
    }

    .store-status {
      background: #d4edda;
      color: #155724;
      padding: 15px;
      border-radius: 8px;
      text-align: center;
      font-weight: 700;
      border: 1px solid #c3e6cb;
    }

    @media (max-width: 768px) {
      .store-content {
        grid-template-columns: 1fr;
        gap: 20px;
      }

      .store-header {
        flex-direction: column;
        text-align: center;
        gap: 15px;
      }

      .store-name {
        font-size: 1.5em;
      }
    }
  </style>
</head>
<body>

  <!-- ================= PREMIUM NAVBAR ================ -->
  <%- include('./partials/navbar') %>
  <!-- =============== END PREMIUM NAVBAR ============== -->

  <!-- ================= STORE VIEW CONTENT ================ -->
  <div class="store-view-page">
    <div class="store-view-container">
      <!-- Store Banner -->
      <div class="store-banner" style="background-image: url('/uploads/<%= store.banner %>');"></div>

      <!-- Store Header -->
      <div class="store-header">
        <div class="store-logo" style="background-image: url('/uploads/<%= store.logo %>');"></div>
        <div class="store-info">
          <h1 class="store-name"><%= store.name %></h1>
          <div class="store-meta">
            <span class="store-category"><%= store.category %></span>
            <span class="store-seller">by <%= seller.name %></span>
          </div>
          <p class="store-description"><%= store.description %></p>
        </div>
      </div>

      <!-- Store Content Grid -->
      <div class="store-content">
        <!-- Main Content -->
        <div class="store-details">
          <!-- About Section -->
          <div class="detail-section">
            <h3>📖 About This Store</h3>
            <p><%= store.seoDescription %></p>
          </div>



          <!-- Store Rules (if any) -->
          <% if (store.rules) { %>
            <div class="detail-section">
              <h3>📋 Store Rules & Policies</h3>
              <div class="store-rules">
                <p><%= store.rules %></p>
              </div>
            </div>
          <% } %>

          <!-- Products Section -->
          <div class="detail-section">
            <h3>🎁 Products (<%= store.items ? store.items.filter(item => item.status === 'active').length : 0 %>)</h3>
            <% if (store.items && store.items.filter(item => item.status === 'active').length > 0) { %>
              <div class="products-grid" style="display: grid; grid-template-columns: repeat(auto-fill, minmax(300px, 1fr)); gap: 20px; margin-top: 20px;">
                <% store.items.filter(item => item.status === 'active').forEach(function(product) { %>
                  <div class="product-card" style="background: white; border: 1px solid #e9ecef; border-radius: 12px; overflow: hidden; box-shadow: 0 2px 8px rgba(0,0,0,0.1); transition: transform 0.3s ease;">
                    <!-- Product Image -->
                    <div style="height: 200px; background-size: cover; background-position: center; background-color: #f8f9fa;" class="product-image">
                      <% if (product.images && product.images.length > 0) { %>
                        <img src="/uploads/<%= product.images[0] %>" alt="<%= product.name %>" style="width: 100%; height: 100%; object-fit: cover;" onerror="this.style.display='none'">
                      <% } %>
                      <div style="width: 100%; height: 100%; display: flex; align-items: center; justify-content: center; color: #6c757d;">
                        <i class="fas fa-image fa-2x"></i>
                      </div>
                    </div>

                    <!-- Product Info -->
                    <div style="padding: 15px;">
                      <h5 style="margin: 0 0 8px 0; font-size: 1.1em; color: #2c3e50;"><%= product.name %></h5>
                      <p style="margin: 0 0 10px 0; color: #7f8c8d; font-size: 0.9em;" title="<%= product.shortDescription %>">
                        <%= product.shortDescription.length > 100 ? product.shortDescription.substring(0, 100) + '...' : product.shortDescription %>
                      </p>

                      <!-- Product Details -->
                      <div style="margin-bottom: 12px;">
                        <small class="text-muted d-block">
                          <i class="fas fa-clock me-1"></i><%= product.completionTime %>h delivery
                        </small>
                        <small class="text-muted d-block">
                          <i class="fas fa-shopping-cart me-1"></i>Sold: <%= product.soldCount || 0 %>
                        </small>
                        <% if (product.numberOfSubscribers) { %>
                          <small class="text-muted d-block">
                            <i class="fas fa-users me-1"></i><%= product.numberOfSubscribers %> subscribers
                          </small>
                        <% } %>
                      </div>

                      <!-- Price and Actions -->
                      <div style="display: flex; justify-content: space-between; align-items: center;">
                        <div>
                          <span style="font-size: 1.3em; font-weight: bold; color: #27ae60;">₽ <%= product.price %></span>
                        </div>
                        <div style="display: flex; gap: 8px;">
                          <a href="/product/<%= storeId %>/<%= product.id %>" class="btn btn-sm btn-outline-primary">
                            <i class="fas fa-eye"></i> View
                          </a>
                          <% if (locals.user && locals.user.balance >= product.price) { %>
                            <a href="/product/<%= storeId %>/<%= product.id %>" class="btn btn-sm btn-success">
                              <i class="fas fa-shopping-cart"></i> Buy Now
                            </a>
                          <% } else if (locals.user) { %>
                            <span class="text-danger text-sm">Low balance</span>
                          <% } else { %>
                            <a href="/login" class="btn btn-sm btn-warning">Login to Buy</a>
                          <% } %>
                        </div>
                      </div>
                    </div>
                  </div>
                <% }); %>
              </div>
            <% } else { %>
              <div style="background: #f8f9fa; border: 2px dashed #dee2e6; border-radius: 8px; padding: 30px; text-align: center; margin-top: 20px;">
                <p style="margin: 0; color: #6c757d;">
                  🛒 No products available at the moment<br>
                  <small>Check back later or contact the seller directly</small>
                </p>
              </div>
            <% } %>
          </div>
        </div>

        <!-- Sidebar -->
        <div class="sidebar">
          <!-- Store Stats -->
          <div class="sidebar-card">
            <h3>📊 Store Statistics</h3>
            <div class="stat-item">
              <span class="stat-label">Products</span>
              <span class="stat-number"><%= store.items ? store.items.length : 0 %></span>
            </div>
            <div class="stat-item">
              <span class="stat-label">Category</span>
              <span class="stat-label" style="color: #27ae60; font-weight: 700; text-transform: capitalize;"><%= store.category %></span>
            </div>
            <div class="stat-item">
              <span class="stat-label">Verified</span>
              <span class="stat-label" style="color: #28a745; font-weight: 700;">✓ Yes</span>
            </div>
          </div>

          <!-- Quick Actions -->
          <div class="sidebar-card">
            <h3>⚡ Quick Actions</h3>
            <div class="action-buttons">
              <a href="/stores" class="action-btn primary">👀 Browse All Stores</a>
              <% if (locals.user && locals.user.isSeller) { %>
                <a href="/my-store" class="action-btn secondary">🏪 My Store</a>
              <% } else { %>
                <a href="/become-seller" class="action-btn secondary">💼 Become a Seller</a>
              <% } %>
            </div>
          </div>

          <!-- Store Status -->
          <div class="sidebar-card">
            <h3>✅ Store Status</h3>
            <div class="store-status">
              🟢 Approved & Active
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>

  <!-- ================= FOOTER ================ -->
  <footer class="footer">
    <div class="footer-content">
      <div class="footer-links">
        <a href="/">Home</a>
        <a href="/stores">Stores</a>
        <a href="#products">Products</a>
        <a href="#games">Games</a>
        <a href="#ai">AI</a>
        <a href="#pricing">Pricing</a>
        <a href="/login">Login</a>
        <a href="/signup">Sign Up</a>
      </div>
      <p>&copy; 2025 DigitalMarket. All rights reserved.</p>
    </div>
  </footer>

</body>
</html>
